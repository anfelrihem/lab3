// echo.js

// Define the function exf
function exf(s, n) {
  for (let i = 0; i < n; i++) {
    console.log(s);
  }
}

// Call the function twice as requested
exf("echo", 5);
exf("JS from server", 10);
